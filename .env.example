# Flask Configuration
FLASK_KEY=your-secret-key-here-change-in-production
DB_URI=postgresql://username:password@host:port/database?sslmode=require

# Email Configuration (for sending contact/password reset emails)
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-specific-password
RECIPIENT_EMAIL=admin@yourdomain.com
